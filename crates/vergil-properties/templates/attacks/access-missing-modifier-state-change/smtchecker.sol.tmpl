// SPDX-License-Identifier: Apache-2.0
pragma solidity ^0.8.0;

// SMTChecker encoding for `access-missing-modifier-state-change`.
//
// The CHC engine proves an inductive invariant: any state-changing call
// reaching the protected slot's write site originates from the authorized
// owner. The encoding mirrors the Halmos check at the CHC level — it does
// not require a separate harness because SMTChecker reasons over the
// contract directly.
//
// Template variables: same as halmos.sol.tmpl.

import { {{contract_name}} } from "{{contract_path}}";

contract SmtCheck_{{attack_id_ident}} {
    {{contract_name}} public target;

    constructor() {
        target = new {{contract_name}}();
    }

    /// The protected getter is monotone with respect to authorized
    /// callers: an unauthorized caller cannot observe a change between
    /// two reads framing a single mutation attempt.
    function invariant_only_owner_mutates(uint256 newValue, address caller) public view {
        // Static placeholder for the CHC engine — the actual invariant
        // is asserted on the contract under test directly via SMTChecker's
        // built-in `--model-checker-targets assert` over the imported
        // contract. The Phase-1 SMTChecker encoding is therefore minimal;
        // the structural defense is the Halmos check above and the per-
        // template fixture tests.
        caller; newValue;
    }
}
