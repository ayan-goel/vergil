# init-double-initialization

Detects re-initialization vulnerabilities (the second call lands). Same fixture surface as Phase 1's `init-unprotected-initializer` (§2.1); kept distinct per attack-patterns.md §2.8 so the bench reports surface both categories. See `manifest.yaml` and `notes/attack-patterns.md` §2.8.
