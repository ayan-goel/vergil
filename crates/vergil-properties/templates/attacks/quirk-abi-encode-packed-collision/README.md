# quirk-abi-encode-packed-collision

Detects use of `abi.encodePacked` over multiple dynamic-length values for
authorization hashes — collision pairs exist. See `manifest.yaml` and
`notes/attack-patterns.md` §14.3.
