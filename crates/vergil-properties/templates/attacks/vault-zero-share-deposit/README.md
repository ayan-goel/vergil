# vault-zero-share-deposit

Detects ERC-4626 deposits that silently mint zero shares (the depositor's
assets are absorbed without representation). See `manifest.yaml` and
`notes/attack-patterns.md` §5.2.
