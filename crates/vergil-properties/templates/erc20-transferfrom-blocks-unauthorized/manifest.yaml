id: erc20-transferfrom-blocks-unauthorized
description: transferFrom must revert when the caller's allowance is strictly less than the requested amount, regardless of the from balance
cost_class: cheap
applies_to:
  interfaces: [ERC20]
requires:
  storage_slots:
    - name: _balances
      type: mapping(address => uint256)
    - name: _allowances
      type: mapping(address => mapping(address => uint256))
  modifiers: []
  external_call_handling: havoc
encoding:
  halmos: halmos.sol
provenance:
  tier: original
  source: vergil-original
  inspired_by: ERC-20 EIP / kill criterion ground truth
  license: Apache-2.0
